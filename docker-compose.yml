version: "3"
services:
  website:
    build:
      context: .
    ports:
      - "5000"
  nginx:
    image: nginx:alpine
    volumes: 
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - website
    ports:
      - "9000:80"

